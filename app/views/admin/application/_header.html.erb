<nav class="metis-navbar d-flex justify-content-between align-items-center">
  <!-- Left: Title or Menu Toggle -->
  <div>
    <h4 class="mb-0">管理画面</h4>
  </div>

  <!-- Right: User -->
  <div class="d-flex align-items-center">
    <!-- User Dropdown -->
    <div class="dropdown">
      <button class="btn btn-link dropdown-toggle" type="button" id="userDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="far fa-user-circle mr-1"></i>
        <span><%= current_admin_user&.email if current_admin_user %></span>
      </button>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
        <h6 class="dropdown-header">アカウント</h6>
        <%= link_to destroy_admin_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item" do %>
          <i class="fas fa-sign-out-alt mr-2"></i> ログアウト
        <% end %>
      </div>
    </div>
  </div>
</nav>
