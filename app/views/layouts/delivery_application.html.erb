<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>配送管理システム - みんなの社食</title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="theme-color" content="#2196f3">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <% if delivery_user_signed_in? %>
      <!-- ヘッダー -->
      <header class="bg-blue-600 text-white fixed top-0 left-0 right-0 z-50 shadow-lg">
        <div class="flex items-center justify-between p-4">
          <h1 class="text-xl font-bold">配送管理</h1>
          <div class="flex items-center gap-4">
            <span class="text-sm"><%= current_delivery_user.name %></span>
            <%= button_to "ログアウト", destroy_delivery_user_session_path, method: :delete, class: "text-sm bg-blue-700 px-3 py-1 rounded hover:bg-blue-800" %>
          </div>
        </div>
      </header>

      <!-- メインコンテンツ -->
      <main class="pt-16 pb-20 min-h-screen">
        <% if notice.present? %>
          <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 m-4 rounded" role="alert">
            <%= notice %>
          </div>
        <% end %>
        <% if alert.present? %>
          <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 m-4 rounded" role="alert">
            <%= alert %>
          </div>
        <% end %>
        <%= yield %>
      </main>

      <!-- フッターナビゲーション -->
      <nav class="bg-white border-t border-gray-200 fixed bottom-0 left-0 right-0 z-50">
        <div class="flex justify-around py-2">
          <%= link_to delivery_root_path, class: "flex flex-col items-center p-2 text-gray-600 hover:text-blue-600 #{current_page?(delivery_root_path) ? 'text-blue-600' : ''}" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
            </svg>
            <span class="text-xs mt-1">ホーム</span>
          <% end %>
          <%= link_to delivery_assignments_path, class: "flex flex-col items-center p-2 text-gray-600 hover:text-blue-600 #{current_page?(delivery_assignments_path) ? 'text-blue-600' : ''}" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
            </svg>
            <span class="text-xs mt-1">配送予定</span>
          <% end %>
          <%= link_to delivery_histories_path, class: "flex flex-col items-center p-2 text-gray-600 hover:text-blue-600 #{current_page?(delivery_histories_path) ? 'text-blue-600' : ''}" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <span class="text-xs mt-1">履歴</span>
          <% end %>
        </div>
      </nav>
    <% else %>
      <!-- 未ログイン時のシンプルなレイアウト -->
      <main class="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 to-blue-100">
        <% if notice.present? %>
          <div class="absolute top-4 left-4 right-4 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded" role="alert">
            <%= notice %>
          </div>
        <% end %>
        <% if alert.present? %>
          <div class="absolute top-4 left-4 right-4 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded" role="alert">
            <%= alert %>
          </div>
        <% end %>
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
